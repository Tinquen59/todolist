import styled from "styled-components";

import colors from "../../../utils/style/colors";

export const CardConexionContainer = styled.div`
    padding: 1.6em 2.2em;
    background-color: ${colors.backgroundLight};
    border: 2px solid ${colors.primary};
    border-radius: 20px;
`;
